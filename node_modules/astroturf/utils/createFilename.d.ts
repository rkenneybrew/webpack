import { ResolvedOptions } from '../types';
export declare function getNameFromFile(fileName: string): string;
export declare function createRequirePath(from: string, to: string): string;
export default function createFilename(hostFile: string, { extension }: ResolvedOptions, id?: string): string;
