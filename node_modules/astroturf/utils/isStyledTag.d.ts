import { NodePath } from '@babel/core';
import * as t from '@babel/types';
import { ResolvedOptions } from '../types';
export default function isStyledTag(tagPath: NodePath<any>, pluginOptions: ResolvedOptions): tagPath is NodePath<t.CallExpression>;
